# Feature Development Architecture Overview

This document provides a visual overview of how features are structured and integrated in the WSO2 Identity Server console.

**Note**: This architecture guide covers feature module structure and i18n/translation flows. Route configuration and console UI integration (routing flow, side panel, access control) are covered in a separate routing integration guide.

## 🏗️ Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           WSO2 Identity Server Console                      │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                        ┌─────────────┴──────────────┐
                        │                            │
                        ▼                            ▼
        ┌───────────────────────────┐   ┌──────────────────────────┐
        │   Apps (Console/MyAccount)│   │   Modules (Shared)       │
        │                           │   │                          │
        │  - Routes Configuration   │   │  - i18n Translations     │
        │  - Path Constants         │   │  - Core Components       │
        │  - Side Panel Config      │   │  - Utilities             │
        │  - Feature Gates          │   │  - React Components      │
        └───────────────┬───────────┘   └───────────┬──────────────┘
                        │                           │
                        │    ┌──────────────────────┘
                        │    │
                        ▼    ▼
        ┌────────────────────────────────────────────────────────┐
        │              Features (Feature Modules)                │
        │                                                        │
        │  admin.example-feature.v1/                            │
        │  ├── constants/     (Feature-specific constants)      │
        │  ├── models/        (TypeScript interfaces)           │
        │  ├── pages/         (Page components)                 │
        │  ├── components/    (UI components)                   │
        │  ├── api/           (API client functions)            │
        │  ├── hooks/         (React hooks)                     │
        │  └── configs/       (Configuration files)             │
        │                                                        │
        │  Each feature is self-contained and modular           │
        └────────────────────────────────────────────────────────┘
```

## 📦 Component Relationships

```
┌──────────────────────────────────────────────────────────────────┐
│                        Feature Module                            │
│  @wso2is/admin.example-feature.v1                               │
└───────────┬──────────────────────────────────────────────────────┘
            │
            │ depends on
            │
            ├─────► @wso2is/i18n                (Translations)
            ├─────► @wso2is/admin.core.v1       (Core functionality)
            ├─────► @wso2is/react-components    (UI components)
            ├─────► @wso2is/access-control      (Permissions)
            └─────► @oxygen-ui/react            (Design system)
```

## 🔄 Feature Integration Flow

```
1. Developer Creates Feature
   └─► features/admin.my-feature.v1/
       ├── constants/
       ├── models/
       └── pages/

2. Add Translations
   └─► modules/i18n/
       ├── models/namespaces/my-feature-ns.ts
       └── translations/en-US/portals/my-feature.ts

3. Register i18n Namespace
   └─► Register in 4 locations:
       ├── modules/i18n/src/constants.ts
       ├── modules/i18n/src/translations/en-US/meta.ts
       ├── features/admin.core.v1/constants/i18n-constants.ts
       └── features/admin.core.v1/configs/app.ts

4. Build & Test
   └─► pnpm build (feature and i18n module)

Note: Route integration (Step 5) covered in separate routing guide
```

## 📋 File Dependencies Map

```
┌─────────────────────────────────────────────────────────────────┐
│                    Feature Page Component                       │
│              (my-feature-page.tsx)                              │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ imports
         │
         ├──► Constants        (./constants/my-feature-constants.ts)
         ├──► Models           (./models/my-feature.ts)
         ├──► Components       (./components/*)
         ├──► Hooks            (./hooks/use-my-feature.ts)
         ├──► API              (./api/my-feature.ts)
         ├──► Translations     (@wso2is/i18n)
         └──► UI Components    (@wso2is/react-components)
```

## 🌐 i18n Translation Flow

```
┌────────────────────────────────────────────────────────────┐
│  Namespace Interface (TypeScript Definition)               │
│  modules/i18n/src/models/namespaces/my-feature-ns.ts      │
│                                                            │
│  export interface MyFeatureNS {                           │
│      page: { title: string; description: string; }        │
│  }                                                         │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ implements
                       │
┌──────────────────────▼─────────────────────────────────────┐
│  Translation File (Actual Strings)                         │
│  modules/i18n/src/translations/en-US/portals/my-feature.ts│
│                                                            │
│  export const myFeature: MyFeatureNS = {                  │
│      page: {                                              │
│          title: "My Feature",                             │
│          description: "Feature description"               │
│      }                                                     │
│  }                                                         │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ exported via
                       │
┌──────────────────────▼─────────────────────────────────────┐
│  Index Files (Module Exports)                             │
│  - models/namespaces/index.ts                             │
│  - translations/en-US/portals/index.ts                    │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ used by
                       │
┌──────────────────────▼─────────────────────────────────────┐
│  Feature Components                                        │
│  const { t } = useTranslation();                          │
│  <h1>{t("myFeature:page.title")}</h1>                     │
└────────────────────────────────────────────────────────────┘
```

## 🛣️ Routing Flow
**Note**: Route configuration is covered in a separate routing integration guide. This diagram is for reference only.
```
User Navigation
      │
      ▼
┌────────────────────────────────┐
│  Console Route Matcher         │
│  (apps/console/configs/        │
│   routes.tsx)                  │
└────────┬───────────────────────┘
         │
         │ matches path
         │
         ▼
┌────────────────────────────────┐
│  Path Constant                 │
│  AppConstants.getPaths()       │
│  .get("MY_FEATURE")            │
│  → "/my-feature"               │
└────────┬───────────────────────┘
         │
         │ loads component
         │
         ▼
┌────────────────────────────────┐
│  Lazy-Loaded Component         │
│  lazy(() => import(            │
│    "@wso2is/admin.my-feature   │
│    .v1/pages/my-feature-page" │
│  ))                            │
└────────┬───────────────────────┘
         │
         │ renders
         │
         ▼
┌────────────────────────────────┐
│  Feature Page Displayed        │
└────────────────────────────────┘
```

## 🔐 Access Control Flow

**Note**: Access control and route protection are covered in the separate routing integration guide. This diagram is for reference only.

```
┌────────────────────────────────┐
│  User Navigates to Feature     │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│  Route Protected Check         │
│  protected: true               │
└────────┬───────────────────────┘
         │
         ├──► Not Authenticated ──► Redirect to Login
         │
         ▼
┌────────────────────────────────┐
│  Feature Gate Check            │
│  (Optional)                    │
└────────┬───────────────────────┘
         │
         ├──► Feature Disabled ──► Show Not Available
         │
         ▼
┌────────────────────────────────┐
│  Permission Check              │
│  (Feature Dictionary)          │
└────────┬───────────────────────┘
         │
         ├──► No Permission ──► Show Access Denied
         │
         ▼
┌────────────────────────────────┐
│  Feature Page Loaded           │
└────────────────────────────────┘
```

## 📊 Data Flow (Typical Feature)

```
┌────────────────────────────────────────────────────────────┐
│                      User Action                           │
│              (Click, Submit Form, etc.)                    │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│                  React Component                           │
│              (Feature Page/Component)                      │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ calls
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│                   Custom Hook                              │
│              (useMyFeature, etc.)                          │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ calls
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│                    API Client                              │
│              (api/my-feature.ts)                           │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ HTTP request
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│                  Backend API                               │
│              (Identity Server)                             │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ response
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│                 State Update                               │
│         (React Query Cache/State)                          │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ re-render
                       │
                       ▼
┌────────────────────────────────────────────────────────────┐
│                   UI Update                                │
│            (Component Re-renders)                          │
└────────────────────────────────────────────────────────────┘
```

## 🎨 Component Hierarchy (Typical Feature)

```
PageLayout (from @wso2is/react-components)
    │
    ├── PageHeader
    │   ├── Title (from i18n)
    │   ├── Description (from i18n)
    │   └── Action Buttons
    │
    ├── FeatureContent
    │   │
    │   ├── ListComponent
    │   │   ├── SearchBar
    │   │   ├── FilterPanel
    │   │   └── DataTable/CardView
    │   │       ├── Row/Card Items
    │   │       └── Pagination
    │   │
    │   ├── FormComponent (Create/Edit)
    │   │   ├── FormFields
    │   │   ├── Validation
    │   │   └── Submit Actions
    │   │
    │   └── DetailComponent
    │       ├── Overview Tab
    │       ├── Settings Tab
    │       └── Advanced Tab
    │
    └── PageFooter
        └── Help Links
```

## 📁 Build & Deployment Flow

```
┌────────────────────────────────┐
│  Source Code                   │
│  features/admin.my-feature.v1/ │
└────────┬───────────────────────┘
         │
         │ pnpm build
         │
         ▼
┌────────────────────────────────┐
│  Rollup Build Process          │
│  - TypeScript compilation      │
│  - ES modules generation       │
│  - Type declarations           │
└────────┬───────────────────────┘
         │
         │ outputs to
         │
         ▼
┌────────────────────────────────┐
│  dist/                         │
│  ├── esm/     (ES modules)     │
│  └── types/   (TypeScript)     │
└────────┬───────────────────────┘
         │
         │ imported by
         │
         ▼
┌────────────────────────────────┐
│  Console App Build             │
│  - Webpack/Vite bundling       │
│  - Code splitting              │
│  - Tree shaking                │
└────────┬───────────────────────┘
         │
         │ outputs to
         │
         ▼
┌────────────────────────────────┐
│  Production Bundle             │
│  - Minified JS                 │
│  - Optimized assets            │
│  - Source maps                 │
└────────────────────────────────┘
```

## 🔑 Key Principles

1. **Modularity**: Features are self-contained packages
2. **Type Safety**: TypeScript throughout
3. **Lazy Loading**: Routes load components on demand
4. **Shared Resources**: Common code in modules/
5. **Centralized Translations**: All i18n in modules/i18n
6. **Access Control**: Permission checks at multiple levels
7. **Build Optimization**: Code splitting and tree shaking

## 📚 Directory Structure Summary

```
identity-apps/
│
├── apps/                          # Application entry points
│   └── console/                   # Main console app
│       ├── src/
│       │   ├── configs/
│       │   │   └── routes.tsx    # Route definitions
│       │   └── features/
│       │       └── admin.core.v1/
│       │           └── constants/
│       │               └── app-constants.ts
│       └── package.json
│
├── features/                      # Feature modules (business logic)
│   ├── admin.example-feature.v1/
│   ├── admin.agents.v1/
│   ├── admin.actions.v1/
│   └── ...
│
├── modules/                       # Shared modules
│   ├── i18n/                     # Translations
│   ├── react-components/         # Reusable UI components
│   └── ...
│
└── package.json                   # Workspace root
```

---

**This architecture enables**:
- 🔄 Independent feature development
- 📦 Code reusability
- 🎯 Easy testing and maintenance
- 🚀 Optimal build performance
- 🌐 Internationalization support
- 🔐 Granular access control

---

For implementation details, refer to:
- [Quick Start Guide](./QUICK_START.md)
- [Integration Guide](./INTEGRATION_GUIDE.md)
- [Feature README](./README.md)
